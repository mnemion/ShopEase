# 모든 웹 크롤러에 대한 설정
User-agent: *

# 개발 환경에서는 검색 엔진 크롤링 차단 (프로덕션에서는 변경 필요)
Disallow: /

# 프로덕션 환경에서는 아래 설정을 사용
# Allow: /
# Disallow: /profile/
# Disallow: /orders/
# Disallow: /checkout/
# Disallow: /cart/
# Disallow: /admin/

# 사이트맵 경로 (프로덕션에서 활성화)
# Sitemap: https://www.shopease.com/sitemap.xml